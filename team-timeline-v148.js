(()=>{
'use strict';const qs=(s,r=document)=>r.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function team(){const q=new URLSearchParams(location.search);return q.get('view')==='teams'?q.get('team'):null}
function items(){try{return news?.items||[]}catch(_){return[]}}
function relevant(c){return items().filter(x=>Array.isArray(x?.teams)&&x.teams.includes(c)).map(x=>({...x,_t:Date.parse(x.published||'')})).sort((a,b)=>(b._t||0)-(a._t||0))}
function fmt(ts){if(!Number.isFinite(ts))return'UNKNOWN TIME';try{return new Intl.DateTimeFormat('en-AU',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date(ts)).toUpperCase()}catch(_){return'UNKNOWN TIME'}}
function tag(x){const c=String(x?.category||'intel').toLowerCase();return`<span class="team-timeline-tag ${esc(c)}">${esc(c.toUpperCase())}</span>`}
function render(){const c=team(),page=qs('#teamHub .team-page');if(!c||!page)return;qs('#edgeTeamTimeline')?.remove();const a=relevant(c),inj=a.filter(x=>x.category==='injury').length,txn=a.filter(x=>x.category==='transaction').length,game=a.filter(x=>x.category==='game').length,sources=new Set(a.map(x=>x.source).filter(Boolean)).size;const html=`<section class="team-timeline" id="edgeTeamTimeline"><div class="team-timeline-head"><div><span>TEAM INTELLIGENCE TIMELINE</span><h3>${esc(c)} verified information wire</h3></div><small>CURATED TRUSTED SOURCES · NEWEST FIRST</small></div><div class="team-timeline-kpis"><div class="team-timeline-kpi"><span>PUBLISHED ITEMS</span><b>${a.length}</b></div><div class="team-timeline-kpi"><span>INJURY</span><b>${inj}</b></div><div class="team-timeline-kpi"><span>TRANSACTION</span><b>${txn}</b></div><div class="team-timeline-kpi"><span>SOURCES</span><b>${sources}</b></div></div>${a.length?`<div class="team-timeline-feed">${a.slice(0,12).map(x=>`<article class="team-timeline-item"><div class="team-timeline-time">${esc(fmt(x._t))}<br>${esc(x.source||'UNKNOWN SOURCE')}</div><div class="team-timeline-body"><b>${esc(x.title||'Untitled intelligence item')}</b><small>${esc(x.summary||'No summary published.')}</small></div>${tag(x)}</article>`).join('')}</div>`:'<div class="team-timeline-empty">NO TEAM-SPECIFIC VERIFIED INTELLIGENCE ITEMS CURRENTLY PUBLISHED</div>'}<div class="team-timeline-policy">INFORMATION IS OPERATIONAL CONTEXT ONLY · IT NEVER MUTATES FROZEN 008A OR REWRITES A PREGAME OPINION</div></section>`;const anchor=qs('#edgeTeamLeague',page)||qs('#edgeTeamConference',page)||qs('#edgeTeamDivision',page)||qs('#edgeTeamTrajectory',page)||qs('#edgeTeamProfile',page);if(anchor)anchor.insertAdjacentHTML('afterend',html);else page.insertAdjacentHTML('beforeend',html)}
function after(){queueMicrotask(render)}document.addEventListener('click',e=>{if(e.target.closest('[data-team],[data-open-team],[data-team-home]'))after()},true);window.addEventListener('popstate',after);document.addEventListener('edgeiq:core-ready',after);document.addEventListener('nflops:information-refresh',after);
function boot(){after();queueMicrotask(()=>{const v=qs('.version');if(v)v.textContent='EDGEiQ NFL · v14.8 STABLE'})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();